"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.writeInvalidGenericArgCountMessage = exports.parseGenericArgs = void 0;var _shared = require("../../reduce/shared.js");
const parseGenericArgs = (name, g, s) => _parseGenericArgs(name, g, s, []);exports.parseGenericArgs = parseGenericArgs;
const _parseGenericArgs = (name, g, s, argNodes) => {
  const argState = s.parseUntilFinalizer();
  argNodes.push(argState.root);
  if (argState.finalizer === ">") {
    if (argNodes.length !== g.params.length) {
      return s.error(writeInvalidGenericArgCountMessage(name, g.names, argNodes.map((arg) => arg.expression)));
    }
    return argNodes;
  }
  if (argState.finalizer === ",")
  return _parseGenericArgs(name, g, s, argNodes);
  return argState.error((0, _shared.writeUnclosedGroupMessage)(">"));
};
const writeInvalidGenericArgCountMessage = (name, params, argDefs) => `${name}<${params.join(", ")}> requires exactly ${params.length} args (got ${argDefs.length}${argDefs.length === 0 ? "" : `: ${argDefs.join(", ")}`})`;exports.writeInvalidGenericArgCountMessage = writeInvalidGenericArgCountMessage; /* v9-4490ac70ee89f910 */
